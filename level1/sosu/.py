# 소수 의사코드
def 소수(숫자):
  if 숫자 == 1:
    소수가아니다!
  for 범위안의숫자 in range(숫자 - 1):
    if 숫자 % 범위안의 숫자 == 0:
      소수가 아니다!
  소수다!

# 소수갯수 세기 의사코드
def 소수갯수세기(숫자):
  소수갯수 = 0
  for in range(숫자 - 1):
    테스트하고자하는숫자 = input()
    if 소수(테스트하고자하는숫자):
      소수갯수 += 1
  return 소수갯수
